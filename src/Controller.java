import SkillDesigner.Skill;

public class Controller {
    private Skill skill;
    public Controller(){

    }
    public void initThread(String skillName){
        //this.skill= new SimpleSkill(skillName) ;
        /*List<String> skills = SimpleSkill.IOLoader.getSkillList();
        for (Iterator<String> iter = skills.iterator(); iter.hasNext(); ) {

            String element = iter.next();
            System.out.println(element);
            // 1 - can call methods of element
            // 2 - can use iter.remove() to remove the current element from the list

            // .../*
        }*/
    }
    public void initThread(){

    }
}
